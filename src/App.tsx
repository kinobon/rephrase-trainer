import RephraseTrainer from "./components/RephraseTrainer";

export default function App() {
  return (
    <main class="bg-base-100 text-base-content min-h-screen">
      <h1 class="pt-6 text-center text-3xl font-bold">LM Studio Chat</h1>
      <RephraseTrainer />
    </main>
  );
}
